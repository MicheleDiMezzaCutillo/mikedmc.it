package it.mikedmc.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import it.mikedmc.model.AofNpc;

public interface AofNpcRepository extends JpaRepository<AofNpc, Long> {
}
